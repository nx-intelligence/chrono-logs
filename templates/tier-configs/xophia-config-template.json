{
  "comment": "Configuration template for Expertise Tier (@sagente/xophia)",
  "comment2": "Copy to project root as xophia-config.json and fill in values",
  
  "<to-do: main-config-key>": {
    "<to-do: setting1>": "ENV::ENV_VARIABLE_NAME",
    "<to-do: setting2>": "value"
  },
  
  "connectionSettings": {
    "comment": "If this tier needs database/external connections",
    "dbConnection": "ENV::DATABASE_URL",
    "apiEndpoint": "ENV::API_ENDPOINT"
  },
  
  "features": {
    "comment": "Feature flags for this tier",
    "featureA": {
      "enabled": true
    },
    "featureB": {
      "enabled": false
    }
  }
}

